---
- name: "Installation des dépendances requises pour GitLab"
  apt:
            name: "curl, openssh-server, ca-certificates, python3-openssl"
            state: present
            update-cache: yes

