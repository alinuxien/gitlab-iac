---
- name: "Installation des dépendances requises pour GitLab"
  apt:
            name: "curl, openssh-server, ca-certificates, python-openssl"
            state: present
            update-cache: yes

